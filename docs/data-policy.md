# Data Policy

Raw data is downloaded locally and usually ignored by git. Processed samples can be committed only if small, legal, and safe. Full data should live under ignored `data/raw` or `data/processed`. Every task must document source, license, access method, expected size, sample availability, and ethics concerns.
