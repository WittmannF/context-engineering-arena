import type {Evidence} from '../types';export function EvidenceTable({items=[]}:{items?:Evidence[]}){return <div className="space-y-3">{items.map(e=><div className="glass rounded-xl p-4" key={e.id}><div className="flex flex-wrap items-center gap-2"><b>{e.id}</b><span className="badge rounded-full px-2 py-0.5 text-xs">{e.source_type}</span><span className="text-xs text-slate-400">{e.date}</span></div><div className="mt-1 font-semibold">{e.title}</div><p className="mt-2 text-sm text-slate-300">{e.excerpt}</p><div className="mt-2 text-xs text-slate-500">{e.location}</div></div>)}</div>}
