export function ScoreBreakdown({score}:{score?:any}){const entries=Object.entries(score?.scores||{});return <div className="glass rounded-2xl p-5"><div className="flex items-center justify-between"><h3 className="font-bold">Score breakdown</h3><div className="text-3xl font-black text-amber-300">{score?.overall_score ?? '—'}</div></div><div className="mt-4 space-y-2">{entries.map(([k,v]:any)=><div key={k}><div className="flex justify-between text-xs"><span>{k.replaceAll('_',' ')}</span><span>{v}</span></div><div className="h-2 rounded bg-slate-800"><div className="h-2 rounded bg-cyan-300" style={{width:`${Math.max(0,Math.min(100,Number(v)))}%`}}/></div></div>)}</div><p className="mt-3 text-sm text-slate-400">{score?.notes}</p></div>}
