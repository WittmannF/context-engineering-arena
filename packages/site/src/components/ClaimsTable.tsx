import type {Claim} from '../types';export function ClaimsTable({claims=[]}:{claims?:Claim[]}){return <div className="space-y-3">{claims.map(c=><div className="glass rounded-xl p-4" key={c.id}><div className="flex flex-wrap gap-2"><span className="badge rounded-full px-2 py-0.5 text-xs">{c.claim_type}</span><span className="rounded-full bg-slate-800 px-2 py-0.5 text-xs">{c.confidence}</span><span className="text-xs text-slate-500">Evidence: {(c.evidence_ids||[]).join(', ')}</span></div><p className="mt-2">{c.claim}</p>{c.notes&&<p className="mt-2 text-sm text-slate-400">{c.notes}</p>}</div>)}</div>}
