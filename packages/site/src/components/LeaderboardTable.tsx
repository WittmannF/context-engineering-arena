export function LeaderboardTable({entries,onOpen,taskId}:{entries:any[];onOpen?:(task:string,pid:string)=>void;taskId:string}){return <div className="glass overflow-hidden rounded-2xl"><div className="grid grid-cols-[70px_1fr_100px] border-b border-slate-800 p-3 text-xs uppercase text-slate-400"><span>Rank</span><span>Participant</span><span>Score</span></div>{entries.map(e=><button key={e.participant_id} onClick={()=>onOpen?.(taskId,e.participant_id)} className="grid w-full grid-cols-[70px_1fr_100px] p-3 text-left hover:bg-slate-800/60"><span>#{e.rank}</span><span>{e.participant_name}</span><b>{e.overall_score ?? 'Missing'}</b></button>)}</div>}
