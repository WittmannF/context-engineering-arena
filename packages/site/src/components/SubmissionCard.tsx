import type {Submission} from '../types';export function SubmissionCard({s,onOpen}:{s:Submission;onOpen:(task:string,pid:string)=>void}){return <button onClick={()=>onOpen(s.task_id,s.participant_id)} className="glass rounded-2xl p-5 text-left hover:border-cyan-300/40"><div className="flex items-center justify-between"><h3 className="font-bold">{s.participant?.display_name||s.participant_id}</h3><span className="rounded-full bg-amber-300 px-3 py-1 text-sm font-bold text-slate-950">{s.score?.overall_score ?? '—'}</span></div><p className="mt-2 text-sm text-slate-400">{s.answer?.title}</p><p className="mt-3 text-xs text-cyan-200">{s.context_trace?.strategy_name}</p></button>}
