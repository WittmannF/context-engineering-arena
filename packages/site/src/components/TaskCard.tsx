import type {Task} from '../types';import { Database } from 'lucide-react';export function TaskCard({task,onOpen}:{task:Task;onOpen:(id:string)=>void}){return <button onClick={()=>onOpen(task.id)} className="glass group rounded-2xl p-5 text-left transition hover:-translate-y-1 hover:border-cyan-300/40"><div className="flex items-center justify-between"><Database className="text-cyan-300"/><span className="rounded-full bg-slate-800 px-2 py-1 text-xs">{task.difficulty}</span></div><h3 className="mt-4 text-xl font-bold group-hover:text-cyan-200">{task.title}</h3><p className="mt-2 text-sm text-slate-400">{task.short_description}</p><div className="mt-4 flex flex-wrap gap-2">{(task.tags||[]).slice(0,4).map(t=><span className="rounded-full bg-slate-800 px-2 py-1 text-xs" key={t}>{t}</span>)}</div><div className="mt-4 text-xs text-slate-500">Dataset: {task.dataset?.name} · {task.corpus_size}</div></button>}
