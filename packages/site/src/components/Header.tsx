import { ShieldCheck } from 'lucide-react';export function Header({page,setPage}:{page:string;setPage:(p:string)=>void}){const nav=[['home','Home'],['tasks','Tasks'],['compare','Compare'],['propose','Propose'],['about','About']];return <header className="sticky top-0 z-50 border-b border-slate-800 bg-[#070b16]/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><button onClick={()=>setPage('home')} className="flex items-center gap-2 font-bold"><ShieldCheck className="text-cyan-300"/>Context Engineering Arena</button><nav className="flex flex-wrap gap-2">{nav.map(([id,label])=><button key={id} onClick={()=>setPage(id)} className={`rounded-full px-3 py-1 text-sm ${page===id?'bg-cyan-300 text-slate-950':'text-slate-300 hover:bg-slate-800'}`}>{label}</button>)}</nav></div></header>}
