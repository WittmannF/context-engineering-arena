import {allTasks,taskSubmissions,leaderboardFor} from '../data';import {TaskCard} from '../components/TaskCard';
export function TasksPage({openTask}:{openTask:(id:string)=>void}){const domains=['all','corporate investigation','finance','open source','public accountability','science','legal','web archaeology'];return <div><h1 className="text-4xl font-black">Tasks</h1><p className="mt-2 text-slate-400">Choose a messy corpus and build a clear evidence-backed page.</p><div className="mt-5 flex flex-wrap gap-2">{domains.map(d=><span key={d} className="rounded-full bg-slate-800 px-3 py-1 text-sm">{d}</span>)}</div><div className="mt-6 grid gap-5 md:grid-cols-3">{allTasks.map(t=><div key={t.id} className="space-y-2"><TaskCard task={t} onOpen={openTask}/><div className="px-2 text-xs text-slate-500">Submissions: {taskSubmissions(t.id).length} · Best score: {leaderboardFor(t.id)[0]?.overall_score ?? '—'}</div></div>)}</div></div>}
