import {Hero} from '../components/Hero';import {TaskCard} from '../components/TaskCard';import {SubmissionCard} from '../components/SubmissionCard';import {allTasks,allSubmissions} from '../data';
export function HomePage({setPage,openTask,openSubmission}:{setPage:(p:string)=>void;openTask:(id:string)=>void;openSubmission:(t:string,p:string)=>void}){return <div className="space-y-10"><Hero setPage={setPage}/><section><h2 className="text-3xl font-bold">Kickstart tasks</h2><div className="mt-5 grid gap-5 md:grid-cols-3">{allTasks.map(t=><TaskCard key={t.id} task={t} onOpen={openTask}/>)}</div></section><section><h2 className="text-3xl font-bold">Latest submissions</h2><div className="mt-5 grid gap-5 md:grid-cols-3">{allSubmissions.slice(0,6).map(s=><SubmissionCard key={s.participant_id+s.task_id} s={s} onOpen={openSubmission}/>)}</div></section></div>}
